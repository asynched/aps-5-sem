import React from 'react'

/**
 * # Loading
 *
 * A UI component that has the content 'loading' inside it.
 *
 * @returns { JSX.Element } A div with 'loading' as a message.
 */
export default function Loading() {
  return <div>Loading...</div>
}
